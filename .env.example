# Database Configuration
# Copy this file to .env before starting the application.
# For Docker Compose deployment, use the postgres service hostname and container port.
DB_HOST=postgres
DB_PORT=5432
DB_NAME=pdao_db
DB_USER=postgres
DB_PASSWORD=thesisit
